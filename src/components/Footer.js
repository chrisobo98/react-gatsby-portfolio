import React from 'react'

// Was in the process of creating another footer but decided the one I have already is ok.
const Footer = (props) => (
    <footer id="footer">
    </footer>
)

export default Footer
